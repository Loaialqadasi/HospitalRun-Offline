# Database URL (PostgreSQL for production/Render)
# For Render: Use the Internal Database URL from your PostgreSQL database
DATABASE_URL=postgresql://username:password@localhost:5432/hospitalrun

# For local development with SQLite (not for production):
# DATABASE_URL=file:./dev.db
